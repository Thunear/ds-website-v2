/**
 * Icon set, sourced from NAV's Aksel design system (@navikt/aksel-icons).
 * Re-exported here so the rest of the app imports from one place and we can
 * remap an icon (e.g. swap which Aksel glyph we use) without touching callers.
 */
export {
  PencilIcon,
  TrashIcon,
  CheckmarkIcon,
  ChevronUpIcon,
  ChevronDownIcon,
  PaletteIcon,
  RaindropIcon,
  CogIcon,
  PlusIcon,
  SunIcon,
  MoonIcon,
  GlobeIcon,
  ArrowUndoIcon,
  FilesIcon as CopyIcon,
  EyeIcon,
  RocketIcon,
  ExpandIcon,
  FileTextIcon,
  SquareIcon,
  XMarkIcon,
} from "@navikt/aksel-icons";
